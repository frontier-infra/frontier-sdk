# frontier-protocol

Python binding for the Frontier SDK protocol. It currently exposes the
`frontier.machine.health.v1` constants and deterministic reference reducer.

This package is provider- and harness-neutral. Passing its fixture suite is not a Machine-L3
deployment claim.
